import org.junit.jupiter.api.Test;

public class IdentifyRequestHandlerTest {
    @Test
    public void testSomething() {
        /**
         * Init embedded messagebus or some object acting as if (mock? Pretty sure it's too convoluted)
         * Add messagelistener
         * Test that listener does stuff
         */
    }
}
